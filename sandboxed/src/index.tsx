import * as React from 'react';
import * as ReactDOM from 'react-dom';
import ParameterActions from './ParameterActions';

ReactDOM.render(<ParameterActions />, document.getElementById('container'));
